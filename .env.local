# API Configuration
# Local development: Use Vite proxy (configured in vite.config.ts)
# Frontend on 5173 proxies /api requests to backend on 5000
# DO NOT set VITE_API_URL here - let api.ts use relative paths with the proxy
